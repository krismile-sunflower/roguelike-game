# Godot 引擎生成的索引文件（不需要提交）
# 这些文件在你的 Godot 版本不同时会不兼容
*.godot
*.godot.user

# project.godot 是项目配置，必须提交（不被上面的规则忽略）
!project.godot

# 导出的项目
export/

# 缓存
.cache/
.editorimportcache/

# 系统文件
.DS_Store
Thumbs.db

# 注意：*.tscn 和 *.gd 需要被 Git 跟踪，所以不忽略
